# Copy to .env and set secure values
PORT=3000
JWT_SECRET=replace-with-strong-secret
# For ADMIN_PASS, you can provide a bcrypt hash (starts with $2) or a plaintext password
ADMIN_USER=admin
ADMIN_PASS=admin123
